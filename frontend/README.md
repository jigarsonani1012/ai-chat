# Frontend

This frontend workspace contains:

- `admin-dashboard`: React app for workspace admins
- `chat-widget`: React widget and embeddable loader entry

## Local development

```bash
npm install
npm run dev:admin
npm run dev:widget
```

Copy each app's `.env.example` to `.env` before local development.
